/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/dnaprs Nextflow configuration
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

params {
    run_name           = 'dnaprs_run'
    outdir             = 'results'
    input              = null
    gwas_manifest      = null
    reference_manifest = null
    manifest_base      = null
    phenotype_file     = null
    phenotype_models   = null
    methods            = 'plink_ct,sbayesrc'
    genome_build       = 'GRCh37'
    seed               = 20260829
    report_enabled     = true

    publish_dir_mode = 'copy'
    email            = null
    email_on_fail    = null
    plaintext_email  = false
    monochrome_logs  = false
    help             = false
    help_full        = false
    show_hidden      = false
    version          = false
    validate_params  = true

    config_profile_name        = null
    config_profile_description = null
    config_profile_contact     = null
    config_profile_url         = null
    custom_config_version      = 'master'
    custom_config_base         = "https://raw.githubusercontent.com/nf-core/configs/${params.custom_config_version}"
}

outputDir = params.outdir
workflow.output.mode = params.publish_dir_mode
workflow.output.overwrite = false

includeConfig 'conf/base.config'

profiles {
    debug {
        dumpHashes = true
        cleanup = false
        nextflow.enable.configProcessNamesValidation = true
    }

    standard {
        process.executor = 'local'
    }

    docker {
        includeConfig 'conf/docker.config'
    }

    apptainer {
        includeConfig 'conf/apptainer.config'
    }

    aqua {
        includeConfig 'conf/aqua.config'
    }

    test {
        includeConfig 'conf/test.config'
    }

    test_full {
        includeConfig 'conf/test.config'
        includeConfig 'conf/test_full.config'
    }
}

// Load nf-core custom profiles from different institutions
includeConfig params.custom_config_base && (!System.getenv('NXF_OFFLINE') || !params.custom_config_base.startsWith('http')) ? "${params.custom_config_base}/nfcore_custom.config" : "/dev/null"

apptainer.registry   = 'quay.io'
singularity.registry = 'quay.io'

env {
    PYTHONNOUSERSITE = '1'
    R_PROFILE_USER   = '/.Rprofile'
    R_ENVIRON_USER   = '/.Renviron'
    LC_ALL           = 'C'
    LANG             = 'C'
}

process.shell = [
    'bash',
    '-C',
    '-e',
    '-u',
    '-o',
    'pipefail',
]

nextflow.enable.configProcessNamesValidation = false

timeline {
    enabled = true
    file = "${params.outdir}/${params.run_name}/reports/provenance/execution_timeline.html"
}

report {
    enabled = true
    file = "${params.outdir}/${params.run_name}/reports/provenance/execution_report.html"
}

trace {
    enabled = true
    file = "${params.outdir}/${params.run_name}/reports/provenance/execution_trace.tsv"
    fields = 'task_id,hash,native_id,name,status,exit,submit,duration,realtime,%cpu,peak_rss,peak_vmem,rchar,wchar,read_bytes,write_bytes,attempt,workdir,container,module'
}

dag {
    enabled = true
    file = "${params.outdir}/${params.run_name}/reports/provenance/pipeline_dag.html"
}

manifest {
    name = 'nf-core/dnaprs'
    contributors = [
        [
            name: 'Paul Ruiz Pinto',
            affiliation: 'Queensland University of Technology',
            email: '',
            github: 'paulYRP',
            contribution: ['author', 'maintainer'],
            orcid: '',
        ],
    ]
    homePage = 'https://github.com/nf-core/dnaprs'
    description = 'Generate and evaluate polygenic risk scores with PLINK and SBayesRC.'
    mainScript = 'main.nf'
    defaultBranch = 'dev'
    nextflowVersion = '!>=25.10.4'
    version = '1.0.0dev'
    doi = ''
}

plugins {
    id 'nf-schema@2.5.1'
}

validation {
    defaultIgnoreParams = ['genomes']
    monochromeLogs = params.monochrome_logs
    logging.unrecognisedParams = 'error'
}

includeConfig 'conf/modules.config'
