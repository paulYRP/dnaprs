name: sbayesrc_tidy
description: Align COJO summary statistics to the official SBayesRC LD reference.
keywords:
  - sbayesrc
  - gwas
  - ld
tools:
  - sbayesrc:
      description: Bayesian multiple regression using summary statistics
      identifier: ""
      homepage: https://github.com/zhilizheng/SBayesRC
  - R:
      description: Statistical computing environment used by the SBayesRC wrapper
      identifier: ""
      homepage: https://www.r-project.org/
input:
  - - meta:
        type: map
        description: GWAS trait and PRS metadata.
    - cojo:
        type: file
        description: Harmonised GWAS table in COJO format.
    - clump_input:
        type: file
        description: Harmonised GWAS table prepared for downstream checks.
    - harmonisation_qc:
        type: file
        description: QC summary from GWAS harmonisation.
    - ld_reference:
        type: map
        description: Prepared SBayesRC LD reference metadata.
    - ld_reference_files:
        type: list
        description: Prepared SBayesRC LD reference files.
  - sbayesrc_script:
      type: file
      description: R script that runs the SBayesRC tidy stage and validates its outputs.
output:
  tidy:
    - - meta:
          type: map
          description: Metadata associated with ld-aligned summary statistics and chained qc.
      - ${meta.trait_id}.tidy.ma:
          type: file
          description: LD-aligned summary statistics and chained QC.
      - ${meta.trait_id}.tidy_qc.tsv:
          type: file
          description: LD-aligned summary statistics and chained QC.
      - harmonisation_qc:
          type: file
          description: LD-aligned summary statistics and chained QC.
  logs:
    - - meta:
          type: map
          description: Metadata associated with sbayesrc tidy-stage log.
      - ${meta.trait_id}.sbayesrc.tidy.log:
          type: file
          description: SBayesRC tidy-stage log.
  versions_sbayesrc:
    - - ${task.process}:
          type: string
          description: The name of the process
      - SBayesRC:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("SBayesRC")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
  versions_r:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: The name of the process
      - SBayesRC:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("SBayesRC")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@paulYRP"
maintainers:
  - "@paulYRP"
