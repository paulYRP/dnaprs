name: assemble_references
description: Assemble validated local and downloaded assets into one reusable reference bundle.
keywords:
  - reference
  - cache
  - provenance
tools:
  - R:
      description: Statistical computing environment
      identifier: ""
      homepage: https://www.r-project.org/
input:
  - asset_rows:
      type: list
      description: Inputs required by the `assemble_references` process.
  - asset_files:
      type: list
      description: Inputs required by the `assemble_references` process.
  - provided_references:
      type: file
      description: User-provided reference records to retain in the bundle.
  - reference_bundle:
      type: file
      description: Inputs required by the `assemble_references` process.
  - genome_build:
      type: string
      description: Inputs required by the `assemble_references` process.
  - cache_root:
      type: string
      description: Inputs required by the `assemble_references` process.
  - assemble_script:
      type: file
      description: Executable script used to produce reference bundle assembly implementation.
output:
  references:
    - references.tsv:
        type: file
        description: Generated canonical reference records.
  bundle:
    - reference_bundle:
        type: directory
        description: Validated reference bundle.
  receipt:
    - reference_receipt.tsv:
        type: file
        description: Reference source, checksum, size, and cache receipt.
  versions_r:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@paulYRP"
maintainers:
  - "@paulYRP"
