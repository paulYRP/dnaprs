name: build_ct_weights
description: Create an allele-explicit PLINK score file from fixed LD-clumped variants.
keywords:
  - plink
  - weights
  - prs
tools:
  - r:
      description: R statistical computing environment
      identifier: ""
      homepage: https://www.r-project.org/
  - data.table:
      description: Software dependency reported by the `build_ct_weights` process
      identifier: ""
      homepage: https://r-datatable.com/
input:
  - - meta:
        type: map
        description: Metadata associated with inputs required by the `build_ct_weights` process.
    - cojo:
        type: file
        description: Inputs required by the `build_ct_weights` process.
    - clumps:
        type: file
        description: Inputs required by the `build_ct_weights` process.
    - harmonisation_qc:
        type: file
        description: Inputs required by the `build_ct_weights` process.
    - clump_log:
        type: file
        description: Inputs required by the `build_ct_weights` process.
  - weight_script:
      type: file
      description: Executable script used to produce r implementation used to create score weights.
output:
  weights:
    - - meta:
          type: map
          description: Metadata associated with allele-explicit score weights, qc, and upstream audit files.
      - ${meta.trait_id}.plink_ct.weights.tsv:
          type: file
          description: Allele-explicit score weights, QC, and upstream audit files.
      - ${meta.trait_id}.plink_ct.weight_qc.tsv:
          type: file
          description: Allele-explicit score weights, QC, and upstream audit files.
      - harmonisation_qc:
          type: file
          description: Allele-explicit score weights, QC, and upstream audit files.
      - clump_log:
          type: file
          description: Allele-explicit score weights, QC, and upstream audit files.
  versions_r:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
  versions_data_table:
    - - ${task.process}:
          type: string
          description: The name of the process
      - data.table:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("data.table")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: The name of the process
      - data.table:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("data.table")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@paulYRP"
maintainers:
  - "@paulYRP"
