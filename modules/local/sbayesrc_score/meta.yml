name: sbayesrc_score
description: Apply posterior SBayesRC weights with the official prs function and no target-derived centring.
keywords:
  - sbayesrc
  - plink
  - scoring
  - prs
tools:
  - sbayesrc:
      description: Bayesian multiple regression using summary statistics
      identifier: ""
      homepage: https://github.com/zhilizheng/SBayesRC
  - plink2:
      description: Whole-genome association and genotype-processing toolset
      identifier: ""
      homepage: https://www.cog-genomics.org/plink/2.0/
  - R:
      description: Statistical computing environment used by the SBayesRC wrapper
      identifier: ""
      homepage: https://www.r-project.org/
input:
  - - target:
        type: map
        description: Target cohort and scoring-stage metadata.
    - target_dir:
        type: directory
        description: Target PGEN dataset used for SBayesRC scoring.
    - target_qc:
        type: file
        description: QC summary for the selected target checkpoint.
    - participant_decisions:
        type: list
        description: Integrated participant eligibility decisions.
    - participant_keep:
        type: file
        description: PLINK keep file for score-eligible participants.
    - gwas:
        type: map
        description: GWAS trait and PRS metadata.
    - weight:
        type: file
        description: Posterior SBayesRC variant weights.
    - parameter:
        type: file
        description: SBayesRC model parameter estimates.
    - model_qc:
        type: file
        description: QC summary from SBayesRC model fitting.
    - impute_qc:
        type: file
        description: QC summary from SBayesRC summary-statistic imputation.
    - tidy_qc:
        type: file
        description: QC summary from SBayesRC summary-statistic preparation.
    - harmonisation_qc:
        type: file
        description: QC summary from GWAS harmonisation.
  - sbayesrc_script:
      type: file
      description: R script that applies SBayesRC weights to the target and validates scores.
output:
  scores:
    - - target:
          type: map
          description: Metadata associated with participant scores and score-coverage qc.
      - gwas:
          type: map
          description: Metadata associated with participant scores and score-coverage qc.
      - ${target.cohort}.${gwas.trait_id}.sbayesrc.score.tsv:
          type: file
          description: Participant scores and score-coverage QC.
      - ${target.cohort}.${gwas.trait_id}.sbayesrc.score_qc.tsv:
          type: file
          description: Participant scores and score-coverage QC.
  logs:
    - - target:
          type: map
          description: Metadata associated with sbayesrc scoring log.
      - gwas:
          type: map
          description: Metadata associated with sbayesrc scoring log.
      - ${target.cohort}.${gwas.trait_id}.sbayesrc.score.log:
          type: file
          description: SBayesRC scoring log.
  versions_sbayesrc:
    - - ${task.process}:
          type: string
          description: The name of the process
      - SBayesRC:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("SBayesRC")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
  versions_plink2:
    - - ${task.process}:
          type: string
          description: The name of the process
      - plink2:
          type: string
          description: The name of the tool
      - command -v plink2 >/dev/null && plink2 --version 2>&1 | head -n 1 | cut -d ' ' -f 2 | sed 's/^v//' || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
  versions_r:
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
topics:
  versions:
    - - ${task.process}:
          type: string
          description: The name of the process
      - SBayesRC:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(packageVersion("SBayesRC")))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: The name of the process
      - plink2:
          type: string
          description: The name of the tool
      - command -v plink2 >/dev/null && plink2 --version 2>&1 | head -n 1 | cut -d ' ' -f 2 | sed 's/^v//' || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
    - - ${task.process}:
          type: string
          description: The name of the process
      - R:
          type: string
          description: The name of the tool
      - Rscript -e 'cat(as.character(getRversion()))' 2>/dev/null || printf stub:
          type: eval
          description: The expression to obtain the version of the tool
authors:
  - "@paulYRP"
maintainers:
  - "@paulYRP"
